# Copy to .env and fill in to run in LIVE mode.
# With no key set, the app runs in deterministic MOCK mode (no cost, no network).

OPENAI_API_KEY=sk-...

# Optional overrides
BDI_MODEL=gpt-4o-mini
# USE_MOCK=1   # force mock mode even if a key is present
